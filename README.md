# Polymetiq Email Services

A basic ready-to-use .NET email services package for local .eml files or networked SMTP messages.

Implementation partialy derived from Orchard Core's internal email implementation, but with an assumed singular email provider.

